# date_input 模块配置
# 复制此文件为 date_input.yaml 并填入真实值

# 数据库配置
database:
  host: "127.0.0.1"
  user: "root"
  passwd: "your_password_here"
  port: 3306
  db: "qqbot"
  charset: "utf8"

# 群关键词配置：群号 -> 触发关键词
group_keywords:
  "群号1": "触发关键词"
  "群号2": "触发关键词"
