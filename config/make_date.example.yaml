# 定时管理模块配置
# 复制此文件为 make_date.yaml 并填入真实值

# 数据库配置
database:
  host: "127.0.0.1"
  user: "root"
  passwd: "your_password_here"
  port: 3306
  db: "qqbot"
  charset: "utf8mb4"

# 管理员群（可执行管理命令的群）
admin_group: []

# 允许使用的群列表
allowed_groups: []
